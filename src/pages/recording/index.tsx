import React from "react";
import { View } from "@tarojs/components";

import "./index.scss";

const Recording = () => {
    return <View className="recording">Recording</View>;
};

export default Recording;
